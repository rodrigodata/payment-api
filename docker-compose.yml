version: '3'
services:
  #implementar pagina de checkout /web
  payment:
    build: './payment'
    ports:
      - "3000:3000"
    depends_on: 
      - db
    environment:
      - PORT=3000
  db:
    image: mongo:latest
    ports:
      - "27017:27017"